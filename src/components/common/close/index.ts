import tw from 'tailwind-styled-components';

const Close = tw.i`
    fas 
    fa-times 
    text-lg
    cursor-pointer
    transition-colors
    duration-200
    hover:text-red-500
`
export default Close;